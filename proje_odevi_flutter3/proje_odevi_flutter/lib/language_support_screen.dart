import 'package:flutter/material.dart';

class LanguageSupportScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dil Desteği'),
      ),
      body: Center(
        child: Text('Dil Desteği Sayfası'),
      ),
    );
  }
}
