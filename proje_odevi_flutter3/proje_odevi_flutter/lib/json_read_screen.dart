import 'package:flutter/material.dart';

class JsonDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Json Veri Okuma'),
      ),
      body: Center(
        child: Text('Json Veri Okuma Sayfası'),
      ),
    );
  }
}
